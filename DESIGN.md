# 실험몽 디자인 시스템

## 1. 비주얼 컨셉 & 경험 방향

실험몽은 단순한 퀴즈 앱이 아니라  
사용자가 “탐색하고, 선택하고, 반응을 체험하는 인터랙티브 경험”이다.

디자인은 정보 전달보다 **몰입감과 리듬감**을 우선한다.  
각 질문은 하나의 “장면(scene)”처럼 느껴져야 하며,  
사용자는 스크롤이 아니라 **흐름을 따라 이동**해야 한다.

전체적인 무드는 다음과 같다:

- 가볍지 않고, 그러나 무겁지도 않은 **세련된 중간 밀도**
- 명확한 구조 위에 **부드러운 애니메이션 레이어**
- 정적인 UI가 아니라 **반응하는 인터페이스**
- 게임처럼 과장되지 않지만, **지루하지 않은 인터랙션**

핵심 키워드:
> **깔끔함 + 몰입감 + 리듬 + 미묘한 감정 반응**

---

## 2. 컬러 시스템

### 기본 배경
- **Main Background**: `#F8F9FB`  
  전체 앱 배경 (따뜻한 오프화이트)
- **Surface**: `#FFFFFF`  
  카드, 질문 영역
- **Elevated Surface**: `#F1F3F8`  
  선택 상태, hover 상태

### 포인트 컬러
- **Primary Blue**: `#4F7CFF`  
  주요 선택, CTA, 진행 상태
- **Accent Violet**: `#7C5CFF`  
  강조 요소, 결과 화면, 감성 포인트
- **Soft Cyan**: `#5EE6D6`  
  보조 강조 (미세 인터랙션, 하이라이트)

### 상태 컬러
- **Success**: `#22C55E`
- **Error**: `#EF4444`
- **Warning**: `#F59E0B`

### 텍스트
- **Primary**: `#222222`
- **Secondary**: `#6B7280`
- **Muted**: `#9CA3AF`
- **Disabled**: `rgba(34,34,34,0.3)`

### 보더 & 효과
- **Default Border**: `rgba(0,0,0,0.08)`
- **Active Border**: `rgba(79,124,255,0.5)`
- **Glow (선택 상태)**: `rgba(79,124,255,0.15)`

---

## 3. 타이포그래피

### 서체
- `Pretendard`, `SUIT`, `Inter`, `system-ui`
- 둥글기보다는 **정제된 현대적인 느낌**

### 계층

| 역할 | 크기 | 굵기 | 특징 |
|------|------|------|------|
| Question Title | 26px | 700 | 핵심 질문, 집중 유도 |
| Section Title | 20px | 600 | 블록 구분 |
| Option Text | 16px | 500 | 선택지 |
| Body | 14px | 400 | 설명 |
| Meta | 12px | 500 | 상태, 보조 |

### 원칙
- 질문은 **짧고 강하게**
- 선택지는 **읽기 쉽고 빠르게 스캔 가능하게**
- 불필요한 장식 없이 **정렬과 간격으로 가독성 확보**

---

## 4. 핵심 인터랙션 설계

### 질문 흐름 (중요)

- 한 화면 = 하나의 질문
- 스크롤이 아니라 **전환(transition)** 중심
- 다음 질문으로 이동 시:
  - 페이드 + 슬라이드 업
  - 200~300ms 이내

👉 사용자는 “페이지 이동”이 아니라  
**“다음 장면으로 넘어간다”**는 느낌을 받아야 한다

---

### 선택지 (Option Card)

- 배경: `#FFFFFF`
- 보더: `1px solid rgba(0,0,0,0.08)`
- 라운드: `16px`
- 패딩: `16px`

#### Hover
- 배경 밝기 미세 상승
- scale: `1.02`
- transition: `120ms ease`

#### 선택 시
- 보더: Primary Blue
- 배경: `#F1F3F8`
- 외곽 glow 효과
- 클릭 시:
  - scale down (0.97) → rebound

👉 핵심: 클릭이 아니라 **“반응한다”는 느낌**

---

### 버튼 (CTA)

#### Primary
- 배경: `#4F7CFF`
- 텍스트: 흰색
- 라운드: `12px`
- hover:
  - 밝기 상승 + soft shadow
- active:
  - 살짝 눌림 효과

#### Secondary
- 배경: 투명
- 보더: 얇은 라인
- hover 시 배경 미세 생성

---

### 진행 상태 (Progress)

- 상단 progress bar
- 부드럽게 채워지는 애니메이션
- step 변화 시:
  - width transition (ease-out)

👉 정적인 게 아니라 “흐름이 보이게”

---

## 5. 애니메이션 원칙

### 기본 규칙
- 빠르되 급하지 않게
- 화려하지 않되 존재감 있게
- 모든 인터랙션에는 **미세한 반응**이 있어야 한다

### 추천 타이밍
- micro interaction: 100~150ms
- hover: 120ms
- 화면 전환: 200~300ms

### 사용 애니메이션
- fade
- translateY (위로 살짝 이동)
- scale (미세 확대/축소)
- opacity 변화

### 금지
- 과한 bounce
- 과한 easing
- 게임 UI 같은 과장된 효과

---

## 6. 레이아웃 구조

### 기본 구조

- 상단: 진행 상태 + 최소 네비게이션
- 중앙: 질문 + 선택지
- 하단: CTA 또는 자동 진행

### 여백 전략
- 질문과 선택지 사이: 넉넉하게
- 선택지끼리는 적당히 촘촘하게
- 전체적으로 **답답하지 않되 집중되는 구조**

---

## 7. 분위기 디테일

### 해야 할 것
- 미묘한 그림자와 glow로 깊이감 추가
- 선택 순간을 “느껴지게” 만들기
- 정적인 화면을 피하고 항상 미세한 변화 유지
- 인터랙션이 자연스럽게 이어지도록 설계

### 피해야 할 것
- 부트스트랩 느낌의 정형화된 버튼/카드
- 과한 라운드 (너무 귀여운 느낌)
- 무채색 위주로만 구성된 심심한 화면
- 클릭해도 반응 없는 UI
- 애니메이션 없는 정적인 흐름

---

## 8. 한 줄 요약

> 실험몽의 UI는 “정보를 보여주는 화면”이 아니라  
> **사용자가 참여하면서 흐름을 체험하는 인터랙티브 공간**이다.